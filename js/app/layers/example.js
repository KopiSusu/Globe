define(['three'], function(THREE) {

    return {
        init: function() {
          //do stuff


          /* Should return a type of Object3D
              e.g. THREE.PointCloud, THREE.Mesh, THREE.Line */
          return 
        }

    }

});